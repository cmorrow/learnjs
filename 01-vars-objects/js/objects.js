let user = {
   name: 'John Smith',
   age: 25,
   isMarried: true
};

// name = ''
function User(name, age, isMarried, []) {
   this.name = name;
   this.age = age;
   this.isMarried = isMarried;
   this.children = children
}

let user1 = new User('Joe', 25, true);
let user2 = new User('April', 34, false);

console.table(user1)
console.table(user2)
